# RAG Database Changelog

All modifications to the RAG vector database (`public.documents`) are documented here.

## [2026-07-12] - Added Zustand Store Pattern Document

### Added
- **Document Title**: `Estructura y Gestión de Estado con Zustand`
- **Metadata**:
  - `company`: `Essertech LLC`
  - `tags`: `["zustand", "state-management", "architecture", "typescript"]`
- **Rationale**:
  - Enables the AI chatbot to accurately answer queries about Daniel's state management practices, including providing a modular TypeScript slice pattern snippet for Zustand stores, which renders correctly via the new premium markdown streaming CSS overrides.
  - Generates the text embedding via `gemini-embedding-2` for compatibility.
